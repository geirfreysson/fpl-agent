# FPL Data Structure Documentation

This document describes the data structure of all parquet files generated by `process_fpl_data.py`.

## Overview

The FPL data processing script converts JSON data into normalized files for efficient analysis:

- **Source Files**: `bootstrap_static.json`, `fixtures.json`, `player_summaries.json`
- **Output Files**: 4 parquet files + 1 JSON file with normalized, analytics-ready data
- **Total Records**: 3,202 records across all files
- **Memory Efficient**: Load only the data you need for specific analyses

## File Structure

```
fpl_data/
├── elements.parquet           # Player basic information and stats
├── fixtures.parquet           # Match fixtures data
├── player_fixtures.parquet    # Player-specific fixture data
├── player_history_past.parquet # Historical season performance
└── teams.json                 # Premier League teams data
```

---

## 1. elements.parquet

**Source**: `bootstrap_static.json` → `elements` section
**Records**: 663 players
**Columns**: 142 columns (including 4 new xG form metrics)  

### Key Columns

#### Player Identity
- `id` (int64): Unique player ID
- `first_name` (str): Player's first name
- `second_name` (str): Player's last name
- `web_name` (str): Display name used in FPL
- `code` (int64): Player code
- `element_type` (int64): Position type (1=GK, 2=DEF, 3=MID, 4=FWD)

#### Team Information
- `team` (int64): Team ID
- `team_code` (int64): Team code
- `squad_number` (str): Jersey number
- `status` (str): Player status (available, injured, etc.)

#### Cost & Ownership
- `now_cost` (int64): Current price (in 0.1m units, e.g., 55 = £5.5m)
- `cost_change_event` (int64): Price change this gameweek
- `cost_change_start` (int64): Price change since season start
- `selected_by_percent` (str): Ownership percentage

#### Performance Stats
- `total_points` (int64): Total FPL points this season
- `event_points` (int64): Points scored in latest gameweek
- `form` (str): Average points per game over last 5 games
- `points_per_game` (str): Average points per game this season
- `minutes` (int64): Total minutes played
- `goals_scored` (int64): Goals scored
- `assists` (int64): Assists provided
- `clean_sheets` (int64): Clean sheets (GK/DEF)
- `goals_conceded` (int64): Goals conceded (GK/DEF)
- `saves` (int64): Saves made (GK)
- `yellow_cards` (int64): Yellow cards received
- `red_cards` (int64): Red cards received
- `bonus` (int64): Bonus points earned
- `bps` (int64): Bonus points system score

#### Advanced Metrics
- `influence` (str): Influence rating
- `creativity` (str): Creativity rating
- `threat` (str): Threat rating
- `ict_index` (str): Combined ICT index
- `expected_goals` (str): Expected goals (xG)
- `expected_assists` (str): Expected assists (xA)
- `expected_goal_involvements` (str): Expected goal involvements
- `expected_goals_conceded` (str): Expected goals conceded

#### Availability
- `can_transact` (bool): Can be bought/sold
- `can_select` (bool): Can be selected in team
- `chance_of_playing_next_round` (float): Injury probability (0-100)
- `news` (str): Latest news/injury updates

### Enhanced Features (Derived Metrics)

The `elements.parquet` file has been enhanced with 28+ additional derived features calculated from the base FPL data. These features provide deeper insights for Fantasy Premier League analysis and are designed to help identify value picks, consistent performers, and players with favorable upcoming fixtures.

#### Value Metrics
These features help identify players offering the best return on investment:

- **`points_per_million`** (float): Total points divided by price in millions. Higher values indicate better value.
- **`form_per_million`** (float): Recent form (last 5 games average) divided by price in millions.
- **`expected_goals_per_million`** (float): Expected goals divided by price in millions. Useful for identifying underpriced attacking threats.

#### Performance Efficiency
Metrics that measure how effectively players use their playing time:

- **`minutes_per_game`** (float): Average minutes played per game. Values close to 90 indicate "nailed-on" status.
- **`points_per_minute`** (float): FPL points earned per minute played. Identifies efficient performers.
- **`goal_involvement_rate`** (float): Percentage of team goals the player was involved in (goals + assists).

#### Expected vs Actual Performance
These features help assess sustainability and identify over/under-performers:

- **`goals_overperformance`** (float): Actual goals minus expected goals. Positive values may indicate unsustainable performance.
- **`assists_overperformance`** (float): Actual assists minus expected assists.
- **`goals_luck_factor`** (float): Ratio of actual to expected goals. Values > 1.2 suggest potential regression.
- **`assists_luck_factor`** (float): Ratio of actual to expected assists.

#### Consistency & Transfer Metrics
Features measuring reliability and market sentiment:

- **`form_consistency`** (float): Standard deviation of last 5 gameweek scores (lower = more consistent).
- **`transfer_momentum`** (int): Net transfers in/out over recent gameweeks.
- **`ownership_category`** (string): Categorized ownership level ("Low", "Medium", "High", "Template").

#### Position-Specific Features
Tailored metrics for different positions:

##### Goalkeepers
- **`save_percentage`** (float): Percentage of shots on target saved.
- **`clean_sheet_rate`** (float): Percentage of games with clean sheets.

##### Defenders
- **`clean_sheet_rate`** (float): Percentage of games with clean sheets.
- **`defensive_value`** (float): Combined metric of defensive stats weighted by price.

##### Midfielders & Forwards
- **`attacking_threat`** (float): Combined metric of attacking stats (goals, assists, shots, key passes).

#### Fixture Difficulty Analysis
Forward-looking metrics based on upcoming fixture difficulty:

- **`avg_fixture_difficulty_3`** (float): Average difficulty of next 3 fixtures (1=easiest, 5=hardest).
- **`avg_fixture_difficulty_5`** (float): Average difficulty of next 5 fixtures.
- **`avg_fixture_difficulty_10`** (float): Average difficulty of next 10 fixtures.
- **`home_fixture_difficulty_5`** (float): Average difficulty of next 5 home fixtures.
- **`away_fixture_difficulty_5`** (float): Average difficulty of next 5 away fixtures.

#### Expected Goals Form (30-Day Rolling)
Rolling averages calculated from matches in the last 30 calendar days:

- **`xg_form_30d`** (float64): Average expected goals per match (last 30 days).
  NaN if no matches in period. Identifies players in good underlying form.

- **`xa_form_30d`** (float64): Average expected assists per match (last 30 days).
  NaN if no matches in period. Indicates creative output potential.

- **`xgi_form_30d`** (float64): Average expected goal involvements per match (last 30 days).
  Sum of xG + xA. Best overall indicator of attacking threat.

- **`matches_last_30d`** (int64): Number of matches played in last 30 days.
  Provides context for reliability. Recommend filtering for >= 3 matches for meaningful analysis.

**Usage Examples**:
```python
# Find in-form attackers
search_players(
    min_xgi_form_30d=0.6,
    min_matches_last_30d=3,
    sort_by="xgi_form_30d"
)

# Identify "due for returns" (high xG form, low actual goals)
search_players(
    min_xg_form_30d=0.4,
    max_goals_scored=2,
    min_matches_last_30d=4
)

# Compare xG form to season totals
players[['web_name', 'xg_form_30d', 'expected_goals', 'goals_scored']]
```

#### Position Rankings
Rank within position group (1 = best in position):

- **`points_rank_in_position`** (int): Rank by total points within position.
- **`value_rank_in_position`** (int): Rank by points per million within position.
- **`form_rank_in_position`** (int): Rank by recent form within position.

#### High-Value FPL Features
Advanced features designed for serious FPL analysis:

##### Per-90 Rate Statistics
Rate stats that account for playing time differences:

- **`goals_per_90`** (float): Goals scored per 90 minutes played. Essential for comparing rotation players.
- **`assists_per_90`** (float): Assists provided per 90 minutes played.
- **`goal_involvements_per_90`** (float): Combined goals + assists per 90 minutes.

##### Captain Analysis
Features for identifying premium captain options:

- **`captain_potential`** (float): Composite score combining ceiling (40%), consistency (30%), reliability (20%), and bonus potential (10%). Higher values indicate better captain options.

##### Set Piece Takers (Heuristic-Based)
Boolean flags identifying likely set piece takers based on statistical patterns:

- **`is_penalty_taker`** (bool): Likely penalty taker based on goals vs expected goals ratio and minimum thresholds.
- **`is_corner_taker`** (bool): Likely corner taker based on assists, creativity stats, and performance thresholds.
- **`is_freekick_taker`** (bool): Likely free kick taker based on threat rating, goals scored, and statistical patterns.

*Note: Set piece flags are intelligent estimates based on player statistics since the FPL API doesn't provide direct set piece role data. Accuracy is high for established takers but should be verified for newer players.*

##### Supporting Metrics
Additional metrics used in calculations:

- **`penalty_potential`** (float): Ratio of actual to expected goals (used for penalty taker detection).
- **`corner_potential`** (float): Assists × creativity metric (used for corner taker detection).
- **`freekick_potential`** (float): Threat × goals metric (used for free kick taker detection).

### Enhanced Usage Examples

```python
# Get all players
players = pd.read_parquet('elements.parquet')

# Find best value midfielders
best_value_mids = players[
    (players['element_type'] == 3) & 
    (players['minutes'] >= 500)
].nlargest(10, 'points_per_million')

# Identify consistent performers
consistent_players = players[
    (players['form_consistency'] <= 2.0) & 
    (players['minutes_per_game'] >= 60)
].nlargest(10, 'total_points')

# Find players with easy upcoming fixtures
easy_fixtures = players[
    players['avg_fixture_difficulty_5'] <= 2.5
].nlargest(10, 'form')

# === NEW HIGH-VALUE FEATURES ===

# Find premium captain options
captain_candidates = players[
    (players['minutes'] >= 1000) &
    (players['total_points'] >= 100)
].nlargest(8, 'captain_potential')

# Identify penalty takers under £10m
penalty_takers = players[
    (players['is_penalty_taker'] == True) &
    (players['now_cost'] <= 100)  # Under £10.0m
].sort_values('total_points', ascending=False)

# Find efficient rotation players (high per-90 stats)
rotation_gems = players[
    (players['minutes'] >= 300) &  # Some playing time
    (players['minutes'] <= 1500) &  # But not always starting
    (players['goals_per_90'] >= 0.4)
].nlargest(10, 'goals_per_90')

# Spot set piece takers with good fixtures
set_piece_kings = players[
    ((players['is_corner_taker'] == True) | 
     (players['is_freekick_taker'] == True) |
     (players['is_penalty_taker'] == True)) &
    (players['avg_fixture_difficulty_5'] <= 3.0)
].nlargest(15, 'total_points')

# Find assist machines (high assists per 90)
assist_providers = players[
    (players['minutes'] >= 500) &
    (players['assists_per_90'] >= 0.3)
].nlargest(10, 'assists_per_90')

# Spot potential value picks (low ownership, high expected goals)
value_picks = players[
    (players['ownership_category'] == 'Low') &
    (players['expected_goals_per_million'] >= 0.5) &
    (players['now_cost'] <= 70)  # Under £7.0m
]

# Find overperforming players (potential sell candidates)
overperformers = players[
    (players['goals_luck_factor'] >= 1.5) &
    (players['total_points'] >= 50)
].sort_values('goals_overperformance', ascending=False)

# Identify nailed-on defenders with good fixtures
nailed_defenders = players[
    (players['element_type'] == 2) &
    (players['minutes_per_game'] >= 80) &
    (players['avg_fixture_difficulty_5'] <= 3.0)
].nlargest(10, 'defensive_value')
```

### Tool Integration

These enhanced features are fully integrated with the FPL analysis tools:

- **`search_players()`**: All enhanced features available as filter parameters including new high-value features
- **`help()`**: Provides tips and example queries using enhanced features

```python
# Example tool usage with NEW features

# Find penalty takers under £8m with good fixtures
search_players(
    is_penalty_taker=True,
    max_price=8.0,
    max_avg_fixture_difficulty_5=3.0,
    sort_by="total_points"
)

# Get top captain options by captain potential
search_players(
    sort_by="captain_potential",
    min_minutes=1000,
    limit=8
)

# Find rotation players with high goals per 90
search_players(
    min_goals_per_90=0.4,
    max_minutes_per_game=75,  # Not always starting
    sort_by="goals_per_90",
    limit=10
)

# Spot set piece takers with easy fixtures
search_players(
    is_corner_taker=True,
    max_avg_fixture_difficulty_5=3.0,
    min_assists_per_90=0.2,
    sort_by="assists_per_90"
)
```

---

## 2. fixtures.parquet

**Source**: `fixtures.json`  
**Records**: 380 fixtures  
**Columns**: 17 columns  

### Key Columns

#### Match Identity
- `id` (int64): Unique fixture ID
- `code` (int64): Match code
- `event` (int64): Gameweek number
- `pulse_id` (int64): Pulse ID

#### Teams
- `team_h` (int64): Home team ID
- `team_a` (int64): Away team ID
- `team_h_score` (object): Home team score (null if not played)
- `team_a_score` (object): Away team score (null if not played)

#### Timing
- `kickoff_time` (str): Match kickoff time (ISO format)
- `minutes` (int64): Minutes played (0 if not started)
- `started` (bool): Whether match has started
- `finished` (bool): Whether match is finished
- `provisional_start_time` (bool): If start time is provisional

#### Difficulty
- `team_h_difficulty` (int64): Home team difficulty rating (1-5)
- `team_a_difficulty` (int64): Away team difficulty rating (1-5)

#### Match Data
- `stats` (object): Match statistics (empty list if not played)

---

## 3. player_fixtures.parquet

**Source**: `player_summaries.json` → `fixtures` section  
**Records**: 1,900 player-fixture combinations  
**Columns**: 15 columns  

### Key Columns

#### Player & Match Identity
- `player_id` (int64): **Player ID (join key)**
- `id` (int64): Fixture ID
- `code` (int64): Match code
- `event` (int64): Gameweek number

#### Teams & Venue
- `team_h` (int64): Home team ID
- `team_a` (int64): Away team ID
- `is_home` (bool): Whether player's team is at home
- `difficulty` (int64): Difficulty rating for player's team (1-5)

#### Match Status
- `kickoff_time` (str): Match kickoff time
- `event_name` (str): Gameweek name (e.g., "Gameweek 1")
- `finished` (bool): Whether match is finished
- `minutes` (int64): Minutes played
- `team_h_score` (object): Home score (null if not played)
- `team_a_score` (object): Away score (null if not played)

### Usage Examples
```python
# Get all fixtures for a specific player
player_381_fixtures = df[df['player_id'] == 381]

# Get all home fixtures
home_fixtures = df[df['is_home'] == True]

# Get easy fixtures (difficulty <= 2)
easy_fixtures = df[df['difficulty'] <= 2]
```

---

## 4. player_history_past.parquet

**Source**: `player_summaries.json` → `history_past` section  
**Records**: 239 player-season combinations  
**Columns**: 29 columns  

### Key Columns

#### Player & Season Identity
- `player_id` (int64): **Player ID (join key)**
- `season_name` (str): Season (e.g., "2024/25", "2023/24")
- `element_code` (int64): Player element code

#### Cost Information
- `start_cost` (int64): Price at season start (0.1m units)
- `end_cost` (int64): Price at season end (0.1m units)

#### Performance Stats
- `total_points` (int64): Total FPL points for the season
- `minutes` (int64): Total minutes played
- `starts` (int64): Games started
- `goals_scored` (int64): Goals scored
- `assists` (int64): Assists provided
- `clean_sheets` (int64): Clean sheets
- `goals_conceded` (int64): Goals conceded
- `saves` (int64): Saves made
- `yellow_cards` (int64): Yellow cards
- `red_cards` (int64): Red cards
- `bonus` (int64): Bonus points
- `bps` (int64): Bonus points system score

#### Advanced Metrics
- `influence` (str): Influence rating
- `creativity` (str): Creativity rating  
- `threat` (str): Threat rating
- `ict_index` (str): ICT index
- `expected_goals` (str): Expected goals
- `expected_assists` (str): Expected assists
- `expected_goal_involvements` (str): Expected goal involvements
- `expected_goals_conceded` (str): Expected goals conceded

### Usage Examples
```python
# Get all historical data for a player
player_history = df[df['player_id'] == 381]

# Get most recent season data
recent_season = df[df['season_name'] == '2024/25']

# Get players with 200+ points in any season
high_scorers = df[df['total_points'] >= 200]
```

---

## 5. teams.json

**Source**: `bootstrap_static.json` → `teams` section  
**Records**: 20 Premier League teams  
**Format**: JSON (not parquet - small, static lookup data)  

### Key Fields

#### Team Identity
- `id` (int): Unique team ID (1-20)
- `name` (str): Full team name (e.g., "Arsenal")
- `short_name` (str): 3-letter team code (e.g., "ARS")
- `code` (int): Team code
- `pulse_id` (int): Pulse identifier

#### League Position & Stats
- `position` (int): Current league position (0 if season not started)
- `played` (int): Games played
- `win` (int): Games won
- `draw` (int): Games drawn
- `loss` (int): Games lost
- `points` (int): League points
- `form` (null): Recent form (null if season not started)

#### Team Strength Ratings
- `strength` (int): Overall team strength (1-5 scale)
- `strength_overall_home` (int): Overall home strength rating
- `strength_overall_away` (int): Overall away strength rating
- `strength_attack_home` (int): Home attack strength
- `strength_attack_away` (int): Away attack strength
- `strength_defence_home` (int): Home defence strength
- `strength_defence_away` (int): Away defence strength

#### Team Status
- `unavailable` (bool): Whether team is unavailable
- `team_division` (null): Team division (null for Premier League)

### Usage Examples
```python
import json

# Load teams data
with open('fpl_data/teams.json', 'r') as f:
    teams = json.load(f)

# Create team lookup dictionary
team_lookup = {team['id']: team for team in teams}

# Get team name by ID
team_name = team_lookup[1]['name']  # "Arsenal"

# Get teams by strength
strong_teams = [team for team in teams if team['strength'] >= 4]

# Get home/away strength differences
for team in teams:
    home_advantage = team['strength_overall_home'] - team['strength_overall_away']
    print(f"{team['short_name']}: {home_advantage} home advantage")
```

---

## Data Relationships

### Joining Data
Use `player_id` to join player-specific data:

```python
import pandas as pd
import json

# Load data
elements = pd.read_parquet('fpl_data/elements.parquet')
player_fixtures = pd.read_parquet('fpl_data/player_fixtures.parquet')
player_history = pd.read_parquet('fpl_data/player_history_past.parquet')

# Load teams data
with open('fpl_data/teams.json', 'r') as f:
    teams_list = json.load(f)
teams_df = pd.DataFrame(teams_list)  # Convert to DataFrame if needed

# Join player info with fixtures
player_fixture_details = player_fixtures.merge(
    elements[['id', 'web_name', 'team', 'element_type']], 
    left_on='player_id', 
    right_on='id'
)

# Join player info with historical performance
player_performance = player_history.merge(
    elements[['id', 'web_name', 'element_type']], 
    left_on='player_id', 
    right_on='id'
)

# Add team names to player data
player_with_teams = elements.merge(
    teams_df[['id', 'name', 'short_name']], 
    left_on='team', 
    right_on='id', 
    suffixes=('', '_team')
)
```

### Key Relationships
- `elements.id` ↔ `player_fixtures.player_id`
- `elements.id` ↔ `player_history_past.player_id`
- `elements.team` ↔ `teams[].id` (team lookup)
- `fixtures.team_h/team_a` ↔ `teams[].id` (team lookup)
- `player_fixtures.team_h/team_a` ↔ `teams[].id` (team lookup)
- `fixtures.id` ↔ `player_fixtures.id` (match-level data)

---

## Memory Usage Tips

### Load Only What You Need
```python
# Load specific columns only
elements_basic = pd.read_parquet(
    'fpl_data/elements.parquet',
    columns=['id', 'web_name', 'now_cost', 'total_points']
)

# Filter while loading
expensive_players = pd.read_parquet(
    'fpl_data/elements.parquet',
    filters=[('now_cost', '>=', 100)]  # £10.0m+
)
```

### Efficient Queries
```python
# Use vectorized operations
top_scorers = elements[elements['total_points'] > 100]

# Use .query() for complex filters
good_fixtures = player_fixtures.query(
    'difficulty <= 3 and is_home == True'
)
```

---

## Data Types Reference

- **int64**: Integer numbers (IDs, scores, counts)
- **float64**: Decimal numbers (percentages, ratings)
- **bool**: True/False values
- **object**: Strings, mixed types, or null values
- **datetime**: Date/time values (stored as strings in ISO format)

---

## Generated by
- **Script**: `process_fpl_data.py`
- **Last Updated**: Generated automatically when script runs
- **Total Processing Time**: ~2-3 seconds for all files
